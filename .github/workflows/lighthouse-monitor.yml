name: Lighthouse monitor

on:
  schedule:
    # 10:07–21:47 every 20 minutes, offset from busy scheduler slots.
    - cron: '7,27,47 10-21 * * *'
      timezone: 'Europe/Prague'
    # 22:07 starts the off-peak 30-minute interval.
    - cron: '7,37 22-23 * * *'
      timezone: 'Europe/Prague'
    # 00:07–09:37 every 30 minutes.
    - cron: '7,37 0-9 * * *'
      timezone: 'Europe/Prague'
  workflow_dispatch:

concurrency:
  group: lighthouse-monitor
  cancel-in-progress: false

permissions:
  contents: write

jobs:
  measure:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Install Chrome
        uses: browser-actions/setup-chrome@v2
        id: setup-chrome

      - name: Run Lighthouse audits
        env:
          CHROME_PATH: ${{ steps.setup-chrome.outputs.chrome-path }}
          METRICS_PATH: public/data/metrics.json
          REPORTS_DIR: artifacts/lighthouse
        run: node scripts/run-lighthouse.mjs

      - name: Upload Lighthouse reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: lighthouse-reports
          path: artifacts/lighthouse
          if-no-files-found: ignore
          retention-days: 3

      - name: Commit metrics
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add public/data/metrics.json
          if git diff --cached --quiet; then
            echo "No metric changes"
            exit 0
          fi
          git commit -m "chore: update lighthouse metrics"
          git pull --rebase --autostash origin main
          git push
